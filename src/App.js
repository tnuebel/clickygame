import React, { Component } from "react";
// import logo from "./logo.svg";
import "./App.css";
// import Card from "./Components/Card";
import Game from "./Components/Game";
// import Wrapper from "./Components/Wrapper";

class App extends Component {
  render() {
    return (
      <Game />
    );
  }
}

export default App;
// I had issues with the 'interactive' aspect of the game...as well as most other areas, as well.
// unable to get the 'Trump cards' to display properly.
// unable to get the cards to randomly rearrange after each selection.
// unable to get the button(s) to work properly.
//
//
